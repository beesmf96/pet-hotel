upload_max_filesize = 20M
post_max_size = 20M
memory_limit = 256M
max_execution_time = 60

; pcov is off by default so it costs nothing on normal requests.
; Coverage runs opt in with `php -d pcov.enabled=1 vendor/bin/phpunit --coverage-text`.
pcov.enabled = 0
pcov.directory = /var/www/app
