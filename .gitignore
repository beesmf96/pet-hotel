*.log
.DS_Store
.claude/settings.local.json
.claude/worktrees/
.rtk/
.env
.env.backup
.env.production
# .env.docker is deliberately NOT ignored — it is a tracked template that README
# tells you to copy to .env. Listing it here while it stayed tracked made it look
# ignored, so a real secret added to it would have been committed silently.
# Keep secrets out of it; it ships in the repo.
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/build
/coverage
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
.gstack/
